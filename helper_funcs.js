

function checkData(respData) {
//    for i in respData.length(){
        console.log(JSON.stringify(respData));
//    }
}

function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

module.exports = checkData, getRandomInt;